conda env remove -n pcdgan
conda create -n pcdgan python=3.10 -y
conda activate pcdgan
# CPU 版 TF（intel 优化，适合 Windows）
pip install "tensorflow-intel==2.14.0"
pip install "tensorflow[and-cuda]==2.14.0" ## Linux only 
# 和 TF 2.14 匹配的 numpy & ml-dtypes
pip install "numpy==1.26.4" "ml-dtypes==0.2.0"

# TensorFlow Addons（会有 EOL warning，忽略即可）
pip install "tensorflow-addons==0.22.0"

# TensorFlow Probability（对应 TF 2.14）
pip install "tensorflow_probability==0.22.1"

pip install scikit-learn matplotlib seaborn tqdm tabulate pillow

conda install -c conda-forge ffmpeg -y (High risk!!!)